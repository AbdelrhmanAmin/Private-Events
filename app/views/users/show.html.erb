<p id="notice"><%= notice %></p>
  <h1>ATTENDING</h1>
  <ul>
      <% @user.attended_events.each do |e| %>
        <li><%= e.title %></li>
      <%end%>
  </ul>
  <h1>Hosting</h1>
    <%# @user.events.each do |e| %>
      <tr>
        <td><%#= e.name %></td>
      </tr>
    <%# end %>

