<p id="notice"><%= notice %></p>
  <h4>ATTENDING</h4>
  <ul>
    <% @user.attended_events.coming_events.each do |e| %>
      <li><%= e.title %></li> <br> 
    <%end%>
  </ul>
  <br>
  <h4>ATTENDED</h4>
  <ul>
    <% @user.attended_events.old_events.each do |e| %>
      <li><%= e.title %></li> <br> 
    <%end%>
  </ul>
  <br>
  <h4>HOSTING</h4>
    <% @user.events.each do |e| %>
      <ul>
        <li><%= e.title %></li><br><br>
      </ul>
    <% end %>

